---
source: tests/snapshot_codegen.rs
expression: output
---
const data = 42;

function transform(x: number): string {
  return "transformed";
}

function format(s: string): string {
  return s;
}

function wrap(prefix: string, value: string, suffix: string): string {
  return prefix;
}

const _result = format(transform(data));

const value = "hello";

const _placed = wrap("(", value, ")");
