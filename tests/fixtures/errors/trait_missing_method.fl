trait Display {
  fn display(self) -> string
}

type User {
  name: string,
  age: number
}

// Missing required method `display`
for User: Display {
  fn toString(self) -> string {
    "wrong method name"
  }
}
