// Record type with deriving Display
type Color {
  r: number,
  g: number,
  b: number,
} deriving (Display)

// Record type with Display only (Eq is built-in via ==)
type User {
  id: string,
  name: string,
  email: string,
} deriving (Display)
