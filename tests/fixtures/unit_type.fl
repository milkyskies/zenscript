// Unit type as return type
function log(msg: string): () {
  console.log(msg)
}

// Unit type in Result generic
function deleteUser(id: string): Result<(), string> {
  Ok(())
}

// Callback with unit return
type ButtonProps = {
  onClick: () => ()
}
