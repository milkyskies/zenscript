const data = 42
function transform(x: number): string { "transformed" }
function format(s: string): string { s }
function wrap(prefix: string, value: string, suffix: string): string { prefix }

const _result = data |> transform |> format

const value = "hello"
const _placed = value |> wrap("(", _, ")")
