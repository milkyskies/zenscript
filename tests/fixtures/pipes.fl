const data = 42
fn transform(x: number) -> string { "transformed" }
fn format(s: string) -> string { s }
fn wrap(prefix: string, value: string, suffix: string) -> string { prefix }

const _result = data |> transform |> format

const value = "hello"
const _placed = value |> wrap("(", _, ")")
