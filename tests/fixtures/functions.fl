export fn add(a: number, b: number) -> number {
    return a + b
}

fn _greet(name: string, greeting: string = "Hello") -> string {
    return `${greeting}, ${name}!`
}

fn _double(n: number) -> number { n * 2 }
