export type Todo = {
    id: string,
    text: string,
    done: boolean,
}

export type Filter =
    | All
    | Active
    | Completed

export type Validation =
    | Valid(text: string)
    | TooShort
    | TooLong
    | Empty

export trait Display {
    fn display(self) -> string
}
